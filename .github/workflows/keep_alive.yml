name: Keep Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # cada 10 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -sf https://clara-certificados.onrender.com/healthz
